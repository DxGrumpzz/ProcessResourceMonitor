<UserControl x:Class="ProcessMonitor.UI.ProcessItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProcessMonitor.UI"
             mc:Ignorable="d" 
             
             d:DesignHeight="450" 
             d:DesignWidth="800"
             
             d:DataContext="{x:Static local:ProcessItemViewModel.DesignTimeData}">

    <Grid>

        <Grid.InputBindings>
            <MouseBinding MouseAction="LeftClick"
                          Command="{Binding}"/>
        </Grid.InputBindings>

        <TextBlock Text="{Binding ProcessData.ProcessName}"/>

    </Grid>

</UserControl>