<UserControl x:Class="ProcessMonitor.UI.ProcessItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProcessMonitor.UI"
             mc:Ignorable="d" 
             
             d:DesignHeight="450" 
             d:DesignWidth="800"
             
             d:DataContext="{x:Static local:ProcessItemViewModel.DesignTimeData}">

    <StackPanel>

        <StackPanel.InputBindings>
            <MouseBinding MouseAction="LeftClick"
                          Command="{Binding}"/>
        </StackPanel.InputBindings>

        
        <TextBlock Text="{Binding ProcessData.ProcessName}"/>
        
        <TextBlock Text="{Binding ProcessData.ProcessHandle}"/>
        
        <TextBlock Text="{Binding ProcessData.ProcessHWND}"/>
        
        <TextBlock Text="{Binding ProcessData.ProcessID}"/>

    </StackPanel>

</UserControl>